{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["index.ts", "package.json", "tsconfig.json", "tsconfig.*.json"]
    },
    "clean": {
      "outputs": []
    }
  }
}
