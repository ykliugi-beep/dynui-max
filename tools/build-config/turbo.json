{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "../../turbo.env.json",
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["index.ts", "package.json", "tsconfig.json", "tsconfig.*.json"]
    },
    "clean": {
      "outputs": []
    }
  }
}
