{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["index.ts", "package.json", "tsconfig.json", "tsconfig.*.json"]
    },
    "clean": {
      "outputs": []
    }
  }
}
